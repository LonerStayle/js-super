# E2E 자기 검증 기록: 무맥락 검증자 병렬

Task 10 의 산출물. 새로 만든 `verifying-spec` 절차를 실제로 한 번 태우고 관찰한 결과.

**대상 문서**: `무맥락-검증자-병렬-implementation-plan.md`
**상위 문서**: `무맥락-검증자-병렬-requirements.md`, `무맥락-검증자-병렬-tech-design.md`
**실행 시점**: Task 1~9 완료 후 (커밋 `3dceb67` 기준)

## 1. dispatch 관찰 (H16 fixture S1 대조)

| S1 기대 항목 | 관찰 | 판정 |
|---|---|---|
| 두 `Agent` 호출이 한 메시지에 묶임 | 한 메시지에 2개 호출 | 통과 |
| 두 호출 모두 백그라운드 | 둘 다 백그라운드 실행 | 통과 |
| `model` 인자 없음 | 둘 다 미지정 (세션 모델 상속) | 통과 |
| 단독 검증자에 상위 문서 경로 미주입 | 대상 문서 1개 경로만 전달 | 통과 |
| 대화 이력 / 작성 의도 / 변경이력 footer 미주입 | 전달 내용은 경로와 판정 기준뿐 | 통과 |
| 보고서에 무맥락 검증 섹션 + 중재 섹션 | 아래 §3, §4 | 통과 |
| 사용자 게이트 1개 | 보고서 하나로 통합 | 통과 |

## 2. 핵심 결과 — 두 판정이 실제로 갈렸는가

**갈렸다.** 수용 기준 6 충족.

메인이 계획서 작성 직후 수행한 자체 검증(`CH-20260815-004` 기록)은 누락 2건을 잡았다 — 개발방향 D4 가 프롬프트에 명시 안 된 점, 수용 기준 1·3·6 을 덮는 task 부재. 모순은 0건으로 판정했다.

무맥락 검증자 둘은 그 2건과 **겹치지 않는** 지적을 합쳐 10건 냈다. 메인이 놓친 것 중에는 배포된 스킬 본문의 실제 설계 구멍이 포함됐다.

메인이 자기 문서를 볼 때와 맥락 없는 에이전트가 볼 때 보이는 것이 다르다는 것이 실측으로 확인됐다.

## 3. 무맥락 검증 결과 (원문)

### 단독 검증자 — 완료 / 지적 8건

- [contradiction] Architecture 줄 "모든 변경이 `skills/verifying-spec/` 안에 갇힌다" 가 `commands/*` 4개, `CLAUDE.md`, fixture 를 고치는 Task 4~9 와 모순
- [contradiction] 중재 표는 지적을 2개 항목으로 남김없이 나누는데, 보고서 형식과 Acceptance 6 은 세 번째 항목 "기각" 이 일상적으로 발생한다고 전제. 기각에 도달하는 경로가 없음
- [contradiction] 단독 검증자의 존재 이유(상위 항목을 다 만족해도 만들 수 없는 문서를 잡는 것)와 중재 규칙("문서 자체 문제 = 수정 사유 아님")이 충돌. 단독 검증자의 지적은 어떤 경우에도 문서를 못 고침
- [ambiguity] "한쪽만 받은 상태로 보고서를 내지 않는다" 와 "응답이 오지 않으면 실패 표기하고 진행" 이 두 갈래로 읽힘. 대기 시간이나 판단 시점 정의 없음
- [contradiction] fixture S2 는 무맥락 검증 섹션이 아예 없는 보고서를 통과시키는데, 스킬 본문과 Acceptance 5 는 건너뜀 한 줄을 요구
- [unbuildable] Task 10 이 "요구사항 수용 기준 1·3·6" 을 근거로 드는데 계획서 안에 그 기준이 인용돼 있지 않음
- [ambiguity] Task 10 에서 양쪽 지적이 완전히 겹치는 경우의 처리 미정의. "결함을 심은 사본" 의 내용·위치·커밋 여부 미정의
- [unverifiable] `git diff --name-only main -- ...` 회귀 검사는 main 머지 후 항상 비어 무의미하고, 무관한 브랜치에서 거짓 실패를 냄

추가 관찰: §2 의 R-3 이 "실행 후 자동 흐름을 한 번 돌려 확인" 이라고만 적고 담당 task 가 없음

### 대조 검증자 — 완료 / 누락 1건, 모순 1건

- **누락** — tech-design §6 R-3 의 확인 절차가 §1 어느 task 에도 배치되지 않음. R-1/R-2/R-4~R-8 은 모두 특정 task·step 에 연결된 반면 R-3 만 서술로 남음
- **모순** — 요구사항 수용 기준 6 과 tech-design §7 은 "실제 피처 하나를 `/tech-design` 까지 끝까지" 를 요구하는데, 계획서 Task 10 은 이 피처 자신의 구현계획서를 대상으로 절차를 수동 재현하는 것으로 대체함. 검증 대상이 tech-design 산출물이 아니고, 슬래시 완주 경로(플래그 인식 → 스킬 진입 → 보고서 → 게이트)가 실행되지 않음

## 4. 중재 결과

두 검증자 모두 R-3 담당 부재를 독립적으로 지적했다. 서로의 결과를 모르는 상태에서 같은 지점에 도달한 것이라 신뢰도가 높다.

### 채택 — 수정함

| 지적 | 조치 |
|---|---|
| 중재 표에 기각 경로 없음 | `skills/verifying-spec/SKILL.md` 중재 절차를 2단계로 재작성 — 1단계 사실 판정(오탐이면 기각 + 사유 기록), 2단계 성격 분리 |
| 단독 검증자 지적이 문서를 못 고침 | 같은 재작성에서 "문서대로 만들 수 없게 만드는 것" 은 `## 권장` 에도 올리도록 단서 추가. 요구사항 FR-5 와 tech-design 도 동기화 |
| "응답 없음" 판단 시점 미정의 | 메인 A + C 검증 종료 시점을 기준으로 명시. 실행 중이면 그 하나를 기다리고, 종료·오류면 그때 확정. 임의 타임아웃은 두지 않음 |
| fixture S2 가 스킬 본문보다 느슨함 | S2 기대 항목에서 "또는" 을 제거하고 건너뜀 한 줄을 필수로 |
| `git diff main` 회귀 검사가 무의미 | 브랜치 무관하게 성립하는 불변식으로 교체 — 네 호출 지점 본문에 프롬프트 경로나 플래그가 등장하면 실패 |
| Architecture 줄 과장 | "dispatch 와 중재 절차가" 로 범위를 좁히고 별도 추가 파일을 명시 |
| R-3 담당 부재 | 계획서 §2 에 "담당: Task 10" 명시 + 정적 확인 방법 기술 |

### 기록만 — 사실이지만 수정 안 함

| 지적 | 사유 |
|---|---|
| Task 10 이 수용 기준 1·3·6 을 인용하지 않음 | Task 10 의 `**검증**:` 필드가 확인할 조건을 자체적으로 다 적고 있어 실행에 지장 없음. 계획서를 상위 문서 인용으로 부풀리지 않는다 |
| Task 10 의 "양쪽 지적 완전 일치" 케이스 미정의 | 이번 실행에서 실제로 갈렸으므로 해당 경로에 진입하지 않았다. 가상의 분기를 지금 정의하면 검증 안 된 규칙이 늘어난다 |

### 미해결 — 사용자 판단 필요

**수용 기준 6 의 "실제 피처 하나를 `/tech-design` 까지 끝까지"** 와 Task 10 의 자기 문서 대상 수동 재현 사이의 간극은 그대로 남는다.

이번 실행이 확인한 것은 dispatch 형태와 두 판정이 갈리는지다. 확인하지 못한 것은 슬래시 완주 경로 전체 — 사용자가 `--no-clean-verify` 를 실제로 입력했을 때 메인이 그걸 알아채는지(R-5), 통합 보고서가 최종 게이트에 실제로 실리는지, 자동 흐름 Step 5 가 확장 보고서를 그대로 노출하는지다.

이건 다음 피처를 `/tech-design` 으로 돌릴 때 자연히 확인된다. 지금 억지로 가짜 피처를 만들어 돌리는 것보다 그쪽이 정확하다.

## 5. 판정

**PASS.** 수용 기준 6 의 핵심 조건(서로 다른 항목 최소 1건 이상)을 크게 넘겨 충족했고, 잡힌 지적 중 5건은 배포된 산출물의 실제 결함이라 그 자리에서 수정했다.

이 기능이 없었으면 위 결함들은 전부 릴리즈에 그대로 실렸을 것이다.
