# 프롬프트 편집 회귀 재검토

## 요약

- 검토 대상: 편집 파일 27개
- 검토 방식: 파일별 git diff 실행 후 diff 기준으로 재검토
- 결과 분포: 정합(clean) 18 / 사소한 다듬기(minor) 5 / 회귀(regression) 4
- high 등급 finding: 2건 (`skills/tech-design/SKILL.md`, `skills/worktree-merge-back/SKILL.md`)

편집 대부분은 기존 문서 내부 모순을 해소하는 방향이라 정합 판정이 많습니다. 다만 아래 4개 파일에서 편집이 새 모순을 만들었습니다. high 2건 먼저 처리하는 것을 권합니다.

## 회귀(regression) — 즉시 조치

### skills/tech-design/SKILL.md — high 1건

- 등급: high (마크다운 구조 깨짐)
- 위치: line 328 (Anti-Patterns 표)
- 문제: risk-annotation 카테고리 개수를 "네 개 → 세 개"로 정정하면서 셀 안에 백틱으로 `side-effect | breaking | race` 를 넣었습니다. GFM 표는 코드 스팬(백틱) 안의 파이프도 컬럼 구분자로 취급합니다. 실제 렌더링 시 Right 컬럼이 `side-effect` 뒤에서 잘리고 `breaking | race` 부분이 별도 컬럼으로 빠져 표에서 사라집니다. 편집 전에는 파이프가 없어 표가 정상이었습니다.
- 수정안: 셀 안 파이프를 이스케이프(`side-effect \| breaking \| race`)하거나 다른 구분자로 교체 (예: `side-effect / breaking / race`).

### skills/worktree-merge-back/SKILL.md — high 1건

- 등급: high (모순 유입)
- 위치: line 166 ("절대 X" 문구) vs line 147 / line 155 예시
- 문제: 166행의 "절대 X" 문구를 "모든 cp 는 prose 출력 후 자동 진행 — blocking-wait 아님"으로 바꿨습니다. 그런데 바로 위 147행은 "기존 키의 값 변경" 케이스에서 사용자 선택을 보존하라고 하고, 155행 예시도 "사용자 확인 필요"라며 실제 응답 대기를 요구합니다. 편집 문구가 "모든 cp 자동 진행"으로 단언해, 애매한 케이스의 사용자 확인 대기를 무력화하는 방향으로 읽힙니다. 이는 secret 오복사 방지라는 이 섹션의 핵심 취지(R-2)와 정면 충돌합니다.
- 수정안: 166행에 "단, 기존 키 값 변경처럼 판단이 애매한 케이스는 실제 사용자 응답을 기다린다"는 예외를 명시하거나, 147/155행 표현을 166행 톤("prose 보고 후 자동 진행, 사용자는 언제든 stop 가능")으로 통일해 두 표현이 같은 동작을 가리키게 하세요.

### commands/sync-html.md — medium 2건

- 등급: medium (모순 유입)
- 위치: line 40 (B-4 룰 1줄 요약 노출) vs line 24 (메인 즉시 다음 turn, 결과 대기 X)
- 문제: "메인은 완료/실패 1줄 요약(예: 완료 (N KB))을 응답에 노출한다"를 새로 넣었는데, 바로 위 24행은 "메인 즉시 다음 turn — 결과 대기 X" 그대로입니다. dispatch 직후 같은 응답에서 실제 파일 크기(N KB)를 알려면 완료 확인이 필요한데, 파일에 delay/체크 단계가 없습니다. auto-brainstorming 등은 B-2 "5초 delay" 같은 단계로 시간을 번 뒤 보고하지만, sync-html.md 는 그 메커니즘 없이 문구만 가져왔습니다.
- 수정안: dispatch 후 간단한 delay 나 `.html` 존재/크기 확인 단계를 Step 4~5 사이에 추가하거나, 확인 불가 시 "백그라운드 호출됨 — 완료 여부 미확인, `/sync-html --check` 로 나중에 조회" 폴백 문구를 명시하세요.

- 등급: medium (파일 내 불일치)
- 위치: line 23 (prompt 항목) vs line 16 (`.html` 존재 케이스) 및 html-companion-prompt.md
- 문제: ".html 존재/부재 두 분기 모두 html-companion-prompt.md 템플릿 기반"이라 명시했지만, 실제 템플릿 Input 에는 기존 .html 내용/디자인을 받아 보존하는 자리표시자가 없습니다. 게다가 그 템플릿의 Design Philosophy 는 "매번 다른 디자인"을 요구해, 16행의 "기존 .html 디자인/레이아웃/색/여백 보존"과 정면으로 어긋납니다.
- 수정안: '.html 존재' 분기는 템플릿 그대로 재사용한다고 하지 말고, 앞에 "기존 .html 의 레이아웃/색/여백을 보존하고 내용만 갱신하라"는 보존 지시문 + 기존 .html 경로/내용을 별도 주입하는 방식임을 명시하거나, html-companion-prompt.md 에 sync 모드용 입력 자리표시자와 분기 규칙을 추가하세요.

### skills/executing-plans/SKILL.md — medium 2건

- 등급: medium (모순 유입)
- 위치: line 253 (Anti-Patterns 표, `git add -A` 행)
- 문제: Right 설명이 "Use explicit file list from Phase 1 tuples + plan.md" 로 plan.md 가 per-task 커밋에 포함된다는 옛 모델을 전제합니다. 바로 위 252행("plan.md must stay untouched during the per-task code-only commits")과 106행("plan.md is NOT included in this commit")에 모순됩니다.
- 수정안: "+ plan.md" 를 제거하고 "Use explicit file list from Phase 1 tuples (code files only — plan.md is committed separately at end-of-run, see Phase 3)." 로 수정.

- 등급: medium (모순 유입)
- 위치: line 254 (Anti-Patterns 표, 'Include plan.md in the git diff extract' 행)
- 문제: Right 설명이 "Plan changes are in the same commit but not in the 변경 전 코드 block" 이라 plan.md 가 코드와 같은 커밋에 들어간다는 옛 모델을 전제합니다. 새 모델은 plan.md 를 별도 end-of-run `[log] all tasks` 커밋으로 완전히 분리하므로 사실이 아닙니다.
- 수정안: "Extract scope = code files only. plan.md changes happen in a separate end-of-run `[log] all tasks` commit (Phase 3), never in the per-task commit or its diff extract." 로 수정.

## 사소한 다듬기(minor)

### commands/remove-skill.md

- 등급: low (파일 내 불일치)
- 위치: §4-2 (line 74) vs §5-3 보고 양식 (line 121-133)
- 요약: "--force 와 함께 와도 --dry-run 우선 — 미리보기만" 규정을 새로 넣었지만, §5-3 보고 양식은 항상 고정 문구 "변경 예정: ...(safe-rename)" 만 보여줍니다. --dry-run + --force 동시 지정 시 실제로는 hard-delete 예정인데 미리보기가 safe-rename 을 표시할 수 있습니다. §5-3 에 "--force 도 지정 시 hard-delete 로 표시" 같은 조건부 안내 한 줄을 더하면 됩니다.

### skills/generating-html/html-companion-prompt.md — 2건

- 등급: medium (모순 유입)
- 위치: line 43 (Allowed enhancements, DAG/flow 행) vs line 113-114 (Self-contained rules)
- 요약: line 43 은 여전히 DAG/flow 를 Mermaid diagram 으로 변환하고 "Mermaid theme 자유 선택"을 필수로 요구합니다. 그러나 편집으로 "Mermaid 런타임 통째 인라인 금지"가 되고 실제 렌더는 `<MERMAID_JS_PATH>` 가 주어질 때만 가능해졌습니다. 채워주는 곳이 없어 line 43 의 필수 지시가 사실상 이행 불가입니다. line 43 을 "inline SVG 로 그리되 색감/톤 자유 선택" 식으로 새 기본값에 맞게 재작성하거나 옵션 조건임을 명시하세요.

- 등급: low (깨진 참조)
- 위치: line 114 (신규 `<MERMAID_JS_PATH>` 조건절)
- 요약: 새 입력 플레이스홀더 `<MERMAID_JS_PATH>` 를 도입했지만, 유일하게 채우는 곳인 SKILL.md Step 2 (line 98/120)는 여전히 `<ABSOLUTE_MD_PATH>` + `<ABSOLUTE_HTML_PATH>` 만 채웁니다. 아무도 안 채우는 죽은 분기입니다. SKILL.md fill-list 에 배선하거나, 계획이 없으면 조건절을 지우고 "항상 inline SVG 또는 코드블록 보존"으로 단순화하세요.

### skills/requesting-code-review/SKILL.md

- 등급: medium (파일 내 불일치)
- 위치: line 39 (`{PLAN_REFERENCE}` 추가) vs line 60-65 (Example 블록)
- 요약: Placeholders 목록에 `{PLAN_REFERENCE}` 를 새로 추가했지만 Example 블록은 그대로라 6개 중 5개만 시연됩니다. 원래 "문서화된 placeholder 는 전부 Example 에 등장"하는 1:1 패턴이 깨졌습니다. Example 에 `PLAN_REFERENCE: docs/superpowers/plans/deployment-plan.md` 같은 줄을 추가하면 됩니다.

### skills/worktree-remove/SKILL.md

- 등급: low (파일 내 불일치)
- 위치: line 98 (--force 옵트인 불릿) vs Step 1 (line 34-49, git status --porcelain)
- 요약: "미커밋 tracked 변경사항은 --force 여도 Step 1 에서 종료"라 했는데, 실제 Step 1 체크는 untracked-but-not-gitignored 파일도 잡습니다. 절차상 동작은 문제없지만(Step 1 이 항상 먼저), 문서 표현이 실제 체크 범위보다 좁습니다. "미커밋 변경사항(tracked/untracked 불문, git status --porcelain non-empty 인 모든 경우)"으로 넓히면 됩니다.

### skills/writing-plans/SKILL.md

- 등급: low (파일 내 불일치)
- 위치: line 285, 294-295 (Process Flow dot 그래프)
- 요약: Checklist 8/9 와 Detailed Step 3 산문은 fire-and-forget 모델로 갱신했지만, 같은 파일 Process Flow dot 그래프 노드는 옛 표현 그대로라 비동기 특성이 안 보입니다. 자매 skill(brainstorming, tech-design)은 그래프 노드 라벨까지 함께 갱신됐는데 writing-plans 만 어긋납니다. 승인 게이트 노드 라벨에 "; .html in bg" 를 붙여 통일하세요.

## 정합(clean) 파일

편집이 새 문제를 만들지 않았고 기존 모순을 잘 해소한 파일들입니다.

- commands/new-skill.md
- commands/pretty-md.md
- commands/worktree-remove.md
- commands/worktree.md
- scripts/detect_auth.py
- skills/api-auto-testing/SKILL.md
- skills/auto-brainstorming/SKILL.md
- skills/auto-executing-plans/SKILL.md
- skills/auto-tech-design/SKILL.md
- skills/auto-writing-plans/SKILL.md
- skills/brainstorming/SKILL.md
- skills/change-history/SKILL.md
- skills/code-pretty/SKILL.md
- skills/generating-html/SKILL.md
- skills/setting-up-worktrees/SKILL.md
- skills/subagent-driven-development/SKILL.md
- skills/systematic-debugging/SKILL.md
- skills/verifying-spec/SKILL.md
