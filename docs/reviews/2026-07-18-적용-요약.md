# 프롬프트 혼동성 검토 — 적용 요약 (2026-07-18)

원본 검토: [2026-07-18-프롬프트-혼동성-검토.md](./2026-07-18-프롬프트-혼동성-검토.md)
편집 회귀 재검토: [2026-07-18-편집-회귀-재검토.md](./2026-07-18-편집-회귀-재검토.md)

## 처리 방식

65개 프롬프트 검토 결과(발견 188건) 중 **정답이 명확한 것만** 수정했습니다. 각 발견을 실제 파일과 대조해 확인한 뒤에만 손댔고, og-* upstream 원본 미러는 CLAUDE.md verbatim 룰대로 건드리지 않았습니다. 편집 후 파일별 `git diff` 기반 정밀 재검토로 제 편집이 유발한 회귀 4건(high 2)을 잡아 모두 수정했습니다.

## 수정한 파일 (26개 skill/command + 스크립트 1)

### 명확한 버그·모순 (수정 완료)

- **`superpowers:` → `js-super:` 네임스페이스** — `requesting-code-review`, `systematic-debugging`, `subagent-driven-development` (이 배포판에 없는 타입이라 dispatch 실패하던 것)
- **generating-html T1 (v2.2.1 fire-and-forget 잔재)** — `generating-html/SKILL.md`(원본 `.md` 덮어쓰기 지시하던 죽은 A 프롬프트 삭제 + 동기 Process Flow/Sanity-Check를 fire-and-forget으로 재작성), `brainstorming`/`tech-design`/`writing-plans`("PRETTIFIED `.md` 검토" → "RAW `.md` 검토, `.html`은 백그라운드 사이드카"), `html-companion-prompt.md`(AC toggle 모순 + mermaid 실현불가 지시)
- **auto-\* frontmatter** — "generating-html 호출 X"인데 실제 Step 4.5/4.6에서 호출하던 3개 정정
- **auto-executing-plans** — 고아 코드펜스(Anti-Patterns 표 렌더링 깨짐) 제거
- **T7 변경이력 배칭** — `change-history`(모드 분기 + `commit_policy: per-task` 네이밍 주의 명시) + `executing-plans`(frontmatter/HARD-GATE/dot 다이어그램/Anti-Patterns를 "git-fast=code-only per-task + end-of-run 단일 log"로 통일)
- **worktree 안전성** — `worktree-merge-back`(env 스캔이 gitignored `.env`를 못 잡던 bash를 `--ignored=matching`으로 수정 + 비활성 게이트 잔재 정리), `setting-up-worktrees`(묻지 않음 vs 응답 catch → 통지 후 자동 복사로 통일), `worktree-remove`(`--force`가 dirty 워크트리를 강제 제거한다는 실현불가 약속 정정 + 셸 변수 유지 주의), `worktree.md`
- **경로 slug (T6)** — `pretty-md`/`sync-html`/`api-auto-testing`의 `docs/features/<slug>/` → `docs/features/<date>-<slug>/`(glob 안내 포함)
- **api-auto-testing** — `detect_auth.py`에 `__main__` 진입점 추가(문서화된 `python -m scripts.detect_auth .`가 출력 0줄이던 것 → 실제 `pattern:` 출력)
- **빌더 명령** — `new-skill`(부정 오류 "검증 X" 정정), `remove-skill`(slug 파싱 규칙 모순 + `--dry-run`/`--force` 우선순위)
- **code-pretty** — Category C 병합 허용 vs FORBIDDEN 병합 금지 예외 명시
- **writing-plans** — 핸드오프가 폐기 방향 `subagent-driven-development`를 권장하던 것 → `js-super-sub-driven`
- **verifying-spec** — dot 다이어그램 고아 노드(결정 경로 끊김) 수정
- **tech-design** — risk 카테고리 "four" → "three"(존재하지 않는 4번째 유발)

## 검증 (실행 기반) — 3 라운드

- 편집 `.md` 코드펜스 균형 전부 OK / 옛 AskUserQuestion 스키마 키 0 / dot 다이어그램 고아 노드 0(writing-plans 4개는 기존 T9 잔재) / `detect_auth.py` 컴파일·CLI 정상
- CLAUDE.md 결합 불변식 유지 (네임스페이스 / generating-html anti-pattern / worktree 안전성 / Other·--no-ask·Checklist 헤더 / dag_builder 테스트)
- **1차 재검토** (편집 27파일 `git diff` 기반): 회귀 4건(high 2) + minor 5건 → 전부 후속 수정
- **2차 재검토** (설계 결정 반영 7파일): 회귀 2건(high 1 — writing-plans Task Model Hint 가 CLAUDE.md `**Model**` 결합 룰 위반) + minor 4건 → 전부 후속 수정. 추가로 writing-plans commit_policy 표·executing-plans Checklist/Remember 의 T7 잔재까지 정리

## 설계 결정 — 사용자 확정 후 적용 완료 (2026-07-18)

아래는 CLAUDE.md 자체가 버전 간 상충이라 코드만으론 못 정하던 것으로, 사용자가 방향을 확정해 그대로 반영했습니다.

1. **auto-\* AskUserQuestion → Socratic prose only (팝업 X)** — `auto-brainstorming`의 "항상 AskUserQuestion" 섹션을 prose-only로 재작성해 frontmatter·Anti-Patterns와 정합화. (나머지 auto-*는 이미 prose였음.)
2. **Implementer 모델 → haiku 고정 (byte-copy 기준)** — `js-super-sub-driven`의 "Model 필드에서 읽어라" 7곳 + `spec-reviewer-prompt.md` 헤더 + `executing-plans` 2곳을 "implementer=haiku 고정 / spec-reviewer=sonnet 고정"으로 통일. plan의 `**Model**:` 필드는 DAG 복잡도 표시용 힌트로만 남김(구현 dispatch 모델을 바꾸지 않음).
3. **AskUserQuestion JSON → 실제 도구 스키마로 정규화** — `brainstorming`/`tech-design`/`writing-plans`의 옛 `{choices:[{value,label}], context}` 예시 8개를 `{options:[{label,description}], header, multiSelect}`로 교체. canonical 블록은 `questions:[...]` 래퍼까지 명시. prose "choices" 용어도 "options"로.

4. **빌더 결정 게이트(T5) → AskUserQuestion 적용** — `new-skill`(생성 스코프 프로젝트/전체), `remove-skill`(§4-0' 양쪽 스코프 존재 시 정리 대상), `change-propagation`(전파 승인: 전체/부분/취소)의 결정 게이트를 prose → `AskUserQuestion` 도구 호출로 전환 (하네스 미지원 시 prose 폴백 유지, "부분 진행"은 multiSelect 후속 질문).

## 알려진 잔여

- (해결됨) `writing-plans` Process Flow dot 그래프의 고아 노드 4개 — 선언부를 엣지가 실제 쓰는 노드명·shape 으로 교체해 정리 완료 (고아 0 / 미선언 0 확인).
