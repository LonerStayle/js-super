---
description: "실행 시점에 모든 워크트리의 git 상태·진행 문서·세션 기록을 수집해 아침 브리핑으로 출력하고, 최상위 워크트리의 .js-super/session-handoff/YYYY-MM-DD.md 노트로 저장. 위험이 있으면 경고 배너를 맨 앞에 표시."
argument-hint: "[추가로 강조하고 싶은 내용 자유 텍스트]"
disable-model-invocation: true
---

# Goodmorning — 아침 핸드오프 브리핑 (자급형)

실행 시점에 각 워크트리의 git 상태·진행 문서·세션 기록을 직접 수집해, 어디까지 했고 다음에 무엇을 해야 하는지를 바로 브리핑합니다. 저녁에 미리 저장해둘 필요가 없습니다. 브리핑 결과는 노트로도 저장해 기록을 남깁니다.

## 1. 최상위 워크트리 파악

1. `git worktree list` 를 실행합니다. 출력 첫 줄의 경로가 최상위 워크트리(main working tree)입니다.
2. 어느 워크트리에서 실행해도 됩니다. 수집 대상 열거와 노트 저장 위치는 항상 최상위 기준입니다.

## 2. 워크트리 열거 + 뒤처진 워크트리 제외

1. `git worktree list` 결과에서 모든 워크트리의 경로·브랜치·HEAD 를 정리합니다.
2. 각 워크트리를 최상위 워크트리의 브랜치와 비교해, 아래 세 조건에 **모두** 해당하면 수집 대상에서 제외합니다.
   - 최상위 브랜치보다 뒤처져 있음 (behind 커밋이 1개 이상)
   - 최상위에 없는 새 커밋이 없음 (ahead 커밋이 0개)
   - 미커밋 변경이 없음 (`git status` 결과가 깨끗함)
3. 하나라도 해당하지 않으면 (새 커밋이 있거나, 미커밋 변경이 있거나) 반드시 수집 대상에 남깁니다.
4. 제외한 워크트리는 브리핑 개요에 한 줄로만 알립니다 (예: "뒤처진 워크트리 2개 제외: A, B").
5. 남은 워크트리 개수 N 을 기록합니다.

## 3. 워크트리별 활동 수집 (병렬 보조 에이전트)

워크트리 하나당 보조 에이전트 하나를 한 메시지에서 병렬로 호출합니다. 각 보조 에이전트에 담당 워크트리 경로를 주고 아래를 조사시킨 뒤, "진행상황 / 다음 할 일 / 주의점 / 위험신호" 네 항목의 구조화 요약만 받습니다.

각 보조 에이전트가 조사할 것:
- git 상태: 현재 브랜치, 최근 커밋 5개, `git status` 로 본 미커밋 변경, 최상위 브랜치 대비 ahead/behind
- 진행 문서: 담당 워크트리의 `docs/features/` 아래 최신 폴더의 requirements/tech-design/implementation-plan 존재 여부와 각 문서 `## 변경이력` 의 최신 항목
- 세션 기록: `~/.claude/projects/` 아래에서 담당 워크트리에 해당하는 세션 폴더의 세션 파일(`.jsonl`)을 분석해 최근 작업 내용·결정·미완료 사항을 파악. **최근 48시간 안에 수정된 세션 파일만 깊게 분석하고, 그보다 오래된 세션은 건너뜁니다** (아침 대기 시간 단축). 폴더 이름은 워크트리 경로가 `-` 로 치환돼 있어 한글 워크트리는 폴더명만으로 구분이 어렵습니다. 세션 파일 안의 실제 작업 경로(cwd) 정보로 담당 워크트리와 매칭하세요.

메인 에이전트는 세션 원문을 직접 읽지 말고 각 보조 에이전트의 요약만 취합합니다.

## 4. 위험 자동 판정 + 경고 배너 결정

취합한 요약을 아래 위험 신호 체크리스트로 점검합니다.
- 미커밋 대량 변경 (수십 개 파일 이상)
- 실패한 테스트 흔적 또는 빌드 깨짐
- 미완료 배포 또는 미완료 마이그레이션
- 해결되지 않은 머지 충돌
- 세션에서 사용자가 남긴 명시적 경고·리마인더 요청

하나라도 해당하면 경고 배너를 만듭니다. 없으면 배너를 만들지 않습니다. 사용자가 커맨드 인자로 강조할 내용을 줬으면 브리핑과 배너 판정에 반영합니다.

경고 배너는 위험이 실제로 있을 때만, 눈에 확 띄게(🚨 마커 + 강한 문장) 만듭니다. 위험이 없으면 배너를 억지로 만들지 않습니다.

## 5. 브리핑 출력

1. 경고 배너가 있으면 브리핑 맨 앞에 크게 먼저 출력합니다.
2. 이어서 개요(최상위 경로, 워크트리 수, 제외한 워크트리)와 워크트리별 "진행상황 / 다음 할 일 / 주의점" 을 순서대로 출력합니다.
3. 경고 배너가 없으면 워크트리별 요약만 담백하게 출력합니다.

## 6. 노트 저장

1. 최상위 워크트리의 `.js-super/session-handoff/` 디렉토리가 없으면 만듭니다.
2. `<최상위>/.js-super/session-handoff/YYYY-MM-DD.md` 에 브리핑 내용을 저장합니다. 같은 날짜 파일이 이미 있으면 덮어써 최신 상태로 갱신합니다.
3. 파일 구조:

```markdown
# 미라클모닝 핸드오프 — YYYY-MM-DD HH:MM

🚨 (위험이 있을 때만) 경고 배너 본문

## 개요
- 최상위 워크트리: <경로>
- 워크트리 수: N (제외 M개: <이름들>)

## 워크트리: <이름> [<브랜치>]
### 진행상황
### 다음 할 일
### 주의점
```

위험이 없으면 배너 줄 자체를 넣지 않습니다. 워크트리마다 위 섹션을 반복하되 서로 섞이지 않게 독립 섹션으로 씁니다.

4. 저장 후 메인 응답 끝에 한 줄로 알립니다: 저장 경로, 워크트리 N개, 경고 있음/없음.

## 7. 출력 스타일 룰 (반드시 지킴)

- 사람이 읽기 위한 명확한 한국어로 씁니다.
- 비유·은유로 설명하지 않습니다. 사실을 그대로 적습니다.
- 불필요한 용어 병기를 하지 않습니다 (예: `안녕(hi)` 처럼 원어를 괄호로 덧붙이지 않음).

## 8. 금지

- 수집 결과에 없는 내용을 지어내지 않습니다.
- 세션 기록 원문을 브리핑이나 노트에 그대로 옮기지 않습니다 (요약만).
- 위험이 없는데 경고 배너를 만들지 않습니다.
- 비유법을 쓰지 않습니다.
