{
  "_주의": "이 파일은 사람만 수정합니다. 레이어 규칙은 설계가 실제로 바뀔 때만 손대는 값입니다. 검사를 통과하려고 규칙을 지우거나 완화하지 마십시오.",
  "_레이어_순서": "먼저 매치되는 레이어가 이깁니다. 글롭의 * 는 / 를 넘어 매치되므로 (scripts/*.py 가 scripts/tests/x.py 도 잡습니다) 면제할 tests 를 반드시 위에 둡니다.",
  "layers": {
    "tests": ["scripts/tests/*", "evals/tests/*"],
    "core": ["scripts/*.py"],
    "harness": ["evals/**"]
  },
  "forbidden": [
    {
      "from": "core",
      "to": "harness",
      "reason": "scripts 의 helper 는 평가 하네스를 몰라야 합니다 (사용자 환경에서도 단독으로 돌아야 하기 때문)"
    }
  ]
}
